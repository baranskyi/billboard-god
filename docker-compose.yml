version: '3.8'

services:
  billboard-tracker:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - SESSION_SECRET=${SESSION_SECRET:-change-this-secret-key}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-admin123}
      - DATA_PATH=./data
      - UPLOADS_PATH=./uploads
    volumes:
      # Постійні дані (зберігаються між перезапусками)
      - billboard_data:/app/data
      - billboard_uploads:/app/uploads
      - billboard_backups:/app/backups
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000', (res) => { process.exit(res.statusCode === 200 ? 0 : 1) })"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Опциональный Nginx для production
  # nginx:
  #   image: nginx:alpine
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #     - ./ssl:/etc/nginx/ssl:ro
  #   depends_on:
  #     - billboard-tracker
  #   restart: unless-stopped

# Named volumes для збереження даних між деплоями
volumes:
  billboard_data:
    driver: local
  billboard_uploads:
    driver: local
  billboard_backups:
    driver: local
